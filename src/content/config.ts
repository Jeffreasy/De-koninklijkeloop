import { defineCollection } from "astro:content";

// ImageKit doesn't have an astro content loader equivalent.
// Photos are fetched directly via the ImageKit SDK in lib/imagekit.ts

export const collections = {};
